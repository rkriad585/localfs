[project]
name = "webplay"
version = "0.1.0"
description = "A Python CLI tool"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
webplay = "webplay.cli:main"

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"
